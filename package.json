{
  "dependencies": {
    "@discordjs/rest": "^2.0.1",
    "discord.js": "^14.13.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "pm2": "^5.3.0",
    "sqlite3": "^5.1.6"
  },
  "scripts": {
    "start": "npx pm2 start index.js",
    "start:watch": "npx pm2 start index.js --watch",
    "restart": "npx pm2 restart index",
    "stop": "npx pm2 stop index",
    "monitor": "npx pm2 monitor index",
    "logs": "npx pm2 logs index"
  },
  "type": "module"
}
